/* 0~9 사이의 숫자로 이루어진 정수 배열에서 0~9 중 없는 숫자만 더한 값 구하기 */
class Solution {
    public int solution(int[] numbers) {
        int answer = 45;
        for (int i = 0; i < numbers.length; i++) {
            answer -= numbers[i];
        }
        return answer;
    }
}
