/* 정수 배열의 요소를 오름차순으로 정렬했을 때, 배열의 정가운데에 위치한 요소 구하기 */
import java.util.*;

class Solution {
    public int solution(int[] array) {
        int answer = 0;
        Arrays.sort(array);
        answer = array[array.length / 2];
        return answer;
    }
}
