/* 배열의 n번째 요소부터 마지막 요소까지로 이루어진 배열 구하기 */
import java.util.*;

class Solution {
    public int[] solution(int[] num_list, int n) {
        int[] answer = {};
        answer = Arrays.copyOfRange(num_list, n - 1, num_list.length);
        return answer;
    }
}
