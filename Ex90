/* 정수 n만큼의 체력을 가진 대상을 공격하려고 한다. 장군개미가 5의 공격력을, 병정개미가 3의 공격력을, 일개미가 1의 공격력을 가지고 있을 때, 가장 적은 수의 필요한 개미 구하기 */
class Solution {
    public int solution(int hp) {
        int answer = 0;
        answer = hp / 5 + (hp % 5) / 3 + (hp % 5 % 3) / 1;
        return answer;
    }
}
