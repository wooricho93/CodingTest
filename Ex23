/* 피자 조각의 개수가 2~10이고 모든 사람들이 한 조각씩 받아야 할 경우 필요한 최소 피자 수 구하기 */
class Solution {
    public int solution(int slice, int n) {
        int answer = 0;
        
        if (n%slice == 0) {
            answer = n / slice;
        } else {
            answer = n / slice + 1;
        }
        
        return answer;
    }
}
